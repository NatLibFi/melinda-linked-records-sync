/* eslint-disable no-unused-vars */
import {Utils} from '@natlibfi/melinda-commons';

const {createLogger} = Utils;
const logger = createLogger();

export function getLinkedInfo(marcRecord) {
	return {linkdata: 'data'};
}
